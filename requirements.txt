# LLM backends — one client class per provider in src/llms/.
# Lazy-imported: only the backend selected by the model id is actually loaded.
anthropic==0.73.0
openai==2.8.0
google-genai==1.50.1
ollama==0.6.1

# API plumbing & forced structured ({"vulnerable": 0|1}) output
httpx==0.28.1
pydantic==2.12.4
python-dotenv==1.2.1

# Dataset construction (src/utils/): call-graph extraction + CodeBLEU ranking
codebleu==0.7.0
tree-sitter-language-pack==1.12.5

# Token counting per model family (src/core/evaluator.py)
tiktoken==0.12.0
transformers==4.57.1

# Metrics, statistics, analysis tables, and figures (src/, expr.ipynb)
numpy==2.3.5
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
statsmodels==0.14.5
matplotlib==3.10.7
prettytable==3.16.0
tqdm==4.67.1

# Notebook that computes the paper's tables and figures (expr.ipynb)
ipykernel==7.1.0
